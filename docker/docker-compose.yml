version: '3'

services:
  vcfstash:
    build:
      context: .
      dockerfile: Dockerfile
    image: vcfstash:latest
    volumes:
      - ${DATA_DIR:-./}:/data
      - ${CACHE_DIR:-./cache}:/cache
      - ${REFERENCE_DIR:-./references}:/references:ro
    working_dir: /data
    environment:
      - VCFSTASH_ROOT=/app
    command: --help

  # Service for running tests
  test:
    image: vcfstash:latest
    volumes:
      - .:/app
    working_dir: /app
    command: test